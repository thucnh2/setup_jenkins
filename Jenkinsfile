pipeline {
    agent any
    stages {
        stage('Clone'){
            steps {
                git branch: 'main', url: 'https://github.com/thucnh2/setup_jenkins.git'
            }
        }
    }
}